

# Update "See How Artist Earnings Work" Button Route

## Change

**File:** `src/components/home/CashBonusFeed.tsx`, line 68

Change the `navigate` target from `"/artist-agreement"` to `"/artist-benefits"`.

```tsx
// Before
onClick={() => navigate("/artist-agreement")}

// After
onClick={() => navigate("/artist-benefits")}
```

Single line change, no other modifications.

