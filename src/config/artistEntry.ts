/**
 * TEMPORARY: Artist application entry points
 * - true  = CTAs + nav go to /artist/apply (testing full flow)
 * - false = waitlist-only before launch (revert here only)
 */
export const ARTIST_APPLICATION_ENTRY_ENABLED = true;
